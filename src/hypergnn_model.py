class HyperGNN:
    """
    Internal architecture intentionally withheld.
    This placeholder keeps the public repo clean while preserving benchmark results.
    """

    def __init__(self, *args, **kwargs):
        raise RuntimeError(
            "HyperGNN architecture is intentionally withheld. "
            "Benchmarks are reported from saved results only."
        )
